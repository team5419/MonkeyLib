package org.team5419.fault.input

/**
 * Holds data about what the Button's State is.
 * @param down Whether the button is down or not.
 * @param pressed Whether the button is pressed or not.
 * @param released Whether the button is released or not.
 */
public data class ButtonState(val down: Boolean, val pressed: Boolean, val released: Boolean)
